# Spaceship Game

## Installation

- Get Go dependencies:

  ```bash
  go get .
  ```

- Build the application:

  ```bash
  go build -o wasm/game.wasm
  ```

- Or run the application in dev mode:

  ```bash
  air
  ```
